---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <div class="min-h-screen px-8 py-10">
    <div class="max-w-4xl mx-auto">
      <h1 class="text-4xl font-bold mb-8">Brainfuck Documentation</h1>
      <div class="space-y-8">
        <section>
          <h2 class="text-2xl font-bold mb-4">What is Brainfuck?</h2>
          <p class="text-lg mb-4">
            Brainfuck is an esoteric programming language created in 1993 by
            Urban Müller. It is designed to be extremely minimalistic, with only
            8 commands that operate on a simple machine model.
          </p>
          <p class="mb-4">
            The language consists of a 30,000-cell array of bytes, a data
            pointer that starts at cell 0, and two streams of bytes for input
            and output.
          </p>
        </section>
        <section>
          <h2 class="text-2xl font-bold mb-4">Commands</h2>
          <div class="bg-[#1a2e2a] rounded-2xl p-6">
            <table class="w-full text-sm">
              <thead>
                <tr class="border-b border-[#25413d]">
                  <th class="text-left py-2 px-3 text-[#b2cfc7]">Command</th>
                  <th class="text-left py-2 px-3 text-[#b2cfc7]">Description</th
                  >
                </tr>
              </thead>
              <tbody>
                <tr class="border-b border-[#22322f]">
                  <td class="py-2 px-3 font-mono text-[#7fffd4]">&gt;</td>
                  <td class="py-2 px-3">Increment data pointer</td>
                </tr>
                <tr class="border-b border-[#22322f]">
                  <td class="py-2 px-3 font-mono text-[#7fffd4]">&lt;</td>
                  <td class="py-2 px-3">Decrement data pointer</td>
                </tr>
                <tr class="border-b border-[#22322f]">
                  <td class="py-2 px-3 font-mono text-[#7fffd4]">+</td>
                  <td class="py-2 px-3">Increment byte at data pointer</td>
                </tr>
                <tr class="border-b border-[#22322f]">
                  <td class="py-2 px-3 font-mono text-[#7fffd4]">-</td>
                  <td class="py-2 px-3">Decrement byte at data pointer</td>
                </tr>
                <tr class="border-b border-[#22322f]">
                  <td class="py-2 px-3 font-mono text-[#7fffd4]">.</td>
                  <td class="py-2 px-3">Output byte at data pointer</td>
                </tr>
                <tr class="border-b border-[#22322f]">
                  <td class="py-2 px-3 font-mono text-[#7fffd4]">,</td>
                  <td class="py-2 px-3">Input byte to data pointer</td>
                </tr>
                <tr class="border-b border-[#22322f]">
                  <td class="py-2 px-3 font-mono text-[#7fffd4]">[</td>
                  <td class="py-2 px-3"
                    >Jump forward if byte at data pointer is zero</td
                  >
                </tr>
                <tr class="border-b border-[#22322f]">
                  <td class="py-2 px-3 font-mono text-[#7fffd4]">]</td>
                  <td class="py-2 px-3"
                    >Jump backward if byte at data pointer is nonzero</td
                  >
                </tr>
              </tbody>
            </table>
          </div>
        </section>

        <section>
          <h2 class="text-2xl font-bold mb-4">Using the Interpreter</h2>
          <div class="space-y-4">
            <div class="bg-[#1a2e2a] rounded-2xl p-6">
              <h3 class="text-xl font-bold mb-3 text-[#7fffd4]">Run Mode</h3>
              <p class="mb-3">
                Execute the entire program at once and see the final output.
              </p>
              <ol class="list-decimal list-inside space-y-1 text-[#b2cfc7]">
                <li>Enter your Brainfuck code in the text area</li>
                <li>Optionally provide input in the input field</li>
                <li>Click "Run" to execute the program</li>
                <li>View the output in the output area</li>
              </ol>
            </div>

            <div class="bg-[#1a2e2a] rounded-2xl p-6">
              <h3 class="text-xl font-bold mb-3 text-[#7fffd4]">Step Mode</h3>
              <p class="mb-3">
                Execute the program one instruction at a time to see how memory
                changes.
              </p>
              <ol class="list-decimal list-inside space-y-1 text-[#b2cfc7]">
                <li>Enter your Brainfuck code and input</li>
                <li>Click "Step" to start stepping mode</li>
                <li>Click "Continue" to execute the next instruction</li>
                <li>Watch the memory visualization update in real-time</li>
                <li>
                  See the current instruction highlighted in the code position
                  indicator
                </li>
              </ol>
            </div>
          </div>
        </section>
      </div>
    </div>
  </div>
</Layout>
